var config =  {};

config.hostName = 'fanghornn.com';

//Set port
config.port = '1338';

//set dev boolean value (if false, minified scripts will load)
config.dev = true;

//Set here your mail acount login (This is used for the web form which allow the user to send me mails from my website)
config.mailLogin = '';

//and your mail account password
config.mailPassword = '';

module.exports = config;
